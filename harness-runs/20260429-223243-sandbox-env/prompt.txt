Re-verify and polish the local Docker Postgres sandbox environment. The feature is partially built but hit runtime issues: @vercel/postgres uses WebSocket (Neon proxy) so seed-rvu.ts can't connect to local Postgres; visit_procedures missing quantity column not in any migration; stale .next/dev/lock not cleaned up. Run the harness to confirm everything works end-to-end: Docker up, migrations, RVU seed, test data seed, dev server starts, sign-in with Dev Login button, visits/favorites/KPIs visible.
