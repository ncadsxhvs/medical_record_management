# =============================================================================
# RVU Tracker - Environment Configuration Template
# =============================================================================
# This file documents all required environment variables.
# Copy to .env.development for local dev or .env.production for production.
# See README for detailed setup instructions.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Neon Postgres)
# -----------------------------------------------------------------------------
# Get these from your Neon project dashboard: https://console.neon.tech/

# Pooled connection URL (use for most queries)
POSTGRES_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require

# Non-pooled connection URL (use for migrations)
POSTGRES_URL_NON_POOLING=postgresql://user:password@host.region.aws.neon.tech/dbname?sslmode=require

# Individual connection parameters
POSTGRES_USER=neondb_owner
POSTGRES_HOST=host-pooler.region.aws.neon.tech
POSTGRES_PASSWORD=your-password-here
POSTGRES_DATABASE=neondb

# Prisma-compatible connection URL (with timeout)
POSTGRES_PRISMA_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?connect_timeout=15&sslmode=require

# Legacy database variables (for compatibility)
DATABASE_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://user:password@host.region.aws.neon.tech/dbname?sslmode=require
PGHOST=host-pooler.region.aws.neon.tech
PGHOST_UNPOOLED=host.region.aws.neon.tech
PGUSER=neondb_owner
PGDATABASE=neondb
PGPASSWORD=your-password-here

# -----------------------------------------------------------------------------
# Auth.js (NextAuth) Configuration
# -----------------------------------------------------------------------------
# Generate a new secret: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here

# Application URL (environment-specific)
# Development: http://localhost:3001
# Production:  https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Google OAuth Credentials
# -----------------------------------------------------------------------------
# Create OAuth 2.0 credentials in Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
#
# IMPORTANT: You MUST create SEPARATE OAuth apps for dev and prod!
#
# Development:
#   - Authorized redirect URI: http://localhost:3001/api/auth/callback/google
#
# Production:
#   - Authorized redirect URI: https://your-app.vercel.app/api/auth/callback/google

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# -----------------------------------------------------------------------------
# Environment-Specific Examples
# -----------------------------------------------------------------------------
#
# .env.development (local development):
#   NEXTAUTH_URL=http://localhost:3001
#   GOOGLE_CLIENT_ID=dev-client-id.apps.googleusercontent.com
#   GOOGLE_CLIENT_SECRET=GOCSPX-dev-secret
#
# .env.production (Vercel deployment):
#   NEXTAUTH_URL=https://hh-ncadsxhvs-projects.vercel.app
#   GOOGLE_CLIENT_ID=prod-client-id.apps.googleusercontent.com
#   GOOGLE_CLIENT_SECRET=GOCSPX-prod-secret
#
# Note: For Vercel, set these in the dashboard instead:
# https://vercel.com/your-team/your-project/settings/environment-variables
# =============================================================================
